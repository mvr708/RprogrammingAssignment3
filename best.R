best <- function(state, outcome){
  if(state !in [list of states]){
    stop("invalid state")
  }
    else { if outcome (!in) [list of outcomes]
      stop("invalid outcome.
            see Hospitals_Revised_Flatfiles.pdf for list of outcomes")
    }
  }